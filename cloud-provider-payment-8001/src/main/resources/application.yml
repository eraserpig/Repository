server:
  port: 8001



spring:
  application:
    name: cloud-payment-service
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource      #当前数据源操作类型
  driver-class-name:  org.git.mm.mysql.Driver         #mysql驱动包
  url:  jdbc:mysql://localhost:3306/dramatic?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: dramatic
    password: dra123
mybatis:
  mapperLocation: classpath:mapper/*.xml
  type-aliases-package: com.data.springcloud.entities     #所有Entity别名类所在包